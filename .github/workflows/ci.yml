name: ci.yml
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Build
        run: ./gradlew clean build

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker Image
        run: |
          docker build \
          -t ghcr.io/maclasoft/my-api:${{ github.sha }} \
          -t ghcr.io/maclasoft/my-api:latest \
          .

      - name: Push Docker Image
        run: |
          docker push ghcr.io/maclasoft/my-api:${{ github.sha }}
          docker push ghcr.io/maclasoft/my-api:latest
